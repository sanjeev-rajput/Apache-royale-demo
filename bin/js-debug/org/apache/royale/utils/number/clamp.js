/**
 * Generated by Apache Royale Compiler from org/apache/royale/utils/number/clamp.as
 * org.apache.royale.utils.number.clamp
 *
 * @fileoverview
 *
 * @suppress {missingRequire|checkTypes|accessControls}
 */

goog.provide('org.apache.royale.utils.number.clamp');
/* Royale Dependency List: */



/**
 *  Returns the value constrained to the range defined by the minimum and maximum values.
 * 
 *  @asparam value The number to constrain.
 *  @asparam min The minimum value.
 *  @asparam max The maximum value.
 *  @asreturn The value constrained to the range defined by the minimum and maximum values.
 * 
 *  @example
 *  <pre>
 *  var value:Number = 5;
 *  var min:Number = 0;
 *  var max:Number = 10;
 *  trace(clamp(value, min, max)); // 5
 *  value = -5;
 *  trace(clamp(value, min, max)); // 0
 *  value = 15;
 *  trace(clamp(value, min, max)); // 10
 *  </pre>
 * @param {number} value
 * @param {number} min
 * @param {number} max
 * @return {number}
 */
org.apache.royale.utils.number.clamp = function(value, min, max) {
  if (value < min)
    return min;
  if (value > max)
    return max;
  return value;
}
